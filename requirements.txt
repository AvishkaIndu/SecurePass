# SecurePass Password Manager Dependencies - Simple Version

# Core cryptography (only external dependency needed)
cryptography>=3.4.8

# Optional: Enhanced password hashing
argon2-cffi>=21.1.0

# Note: This version uses tkinter (built into Python) instead of PyQt5
# No additional GUI dependencies required!